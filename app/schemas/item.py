# This file is intentionally left empty as we are using SQLModel which combines models and schemas.
# If strict separation is needed, we can move the Pydantic-specific parts here.
# For now, keeping everything in models/item.py for simplicity as per SQLModel documentation.
